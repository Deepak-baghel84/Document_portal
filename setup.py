from setuptools import setup, find_packages

setup(
    name='document_portal',
    auther = 'Deepak_Baghel',
    version='0.1',
    packages=find_packages()      # Automatically find packages in the current directory wgich contains an __init__.py file
   
)